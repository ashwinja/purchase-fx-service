# Default settings shared by every environment.
server.port=8080
purchase.storage.file=data/purchases.jsonl

# Live Treasury FiscalData endpoint. The application always calls this API for conversions.
treasury.api.base-url=https://api.fiscaldata.treasury.gov/services/api/fiscal_service/v1/accounting/od/rates_of_exchange
